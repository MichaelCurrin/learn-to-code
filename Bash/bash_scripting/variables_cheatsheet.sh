# This is a last resort in checks, as it specifies your default shell and not necessarily the current shell.
echo $SHELL
# /bin/bash

echo $BASH
# /bin/bash

echo $BASH_VERSION
# 4.4.12(1)-release

echo $ZSH_VERSION
#
