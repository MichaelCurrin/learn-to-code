/**
 * Initialization
 */

// Defaults to value of `undefined`
var myUndefinedVariable;

var myNull = null;

// Create multiple variables at once.
var x, y, z;

var x = 'a',
    y = 'b';

/**
 * Booleans
 */
var x = true;
var y = false;


/**
 * Numbers and operators.
 */
var myInt = 10;
var myFloat = 10.213;

var x = 5,
    y = 10;
add = x + y;
sub = x - y;
div = x / y;
mult = x * y;
exp = x ** y;
mod = x % y;

// Increment.
x++;
// Decrement.
y--;
var a = x++;
var b = ++x;

var rand = Math.random();
var randInt = Math.floor(Math.random() * 50);


/**
 * String operations
 */
var myString = "Double quotes";
myString = 'Single quotes';
var myStringConcat = 'Value: ' + myInt;
var anotherString = 'Start' +
    ' middle ' +
    ' end';
var multilineString = 'A\nB\C';
var anotherMulti = `a
  b
  c
`;
