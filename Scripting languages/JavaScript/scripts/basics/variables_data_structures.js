
/**
 * Arrays
 */
var myArrary = [];
var myNextArray = [1, 3, 55.2, 10, 5, 'text'];
var myNestedArray = [
    [1, 2, 3],
    [4, 5, 6]
]

var myValues = ['A', 'B', 'C'];
// Add to array.
myValues.push('D');
myValues.push('E');
myValues.push('F');
var secondElement = myValues[1];
// Remove last element from array.
var lastElement = myValues.pop();

/**
 * Maps
 * These are key value pairs, known as dictionaries in some languages.
 */
var myMap = {
    a: 'X',
    b: 'Y',
    c: 'Z'
}
